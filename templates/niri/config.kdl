input {
    keyboard {
        xkb {
            layout "us,ru"
            options "grp:caps_toggle"
        }
    }
    touchpad { tap; natural-scroll; }
}
prefer-no-csd
screenshot-path null
animations { off; }
layout {
    gaps 4
    background-color "{{background}}"
    focus-ring { off; }
    border {
        width 1
        active-color "{{accent}}"
        inactive-color "{{border}}"
    }
    shadow { off; }
    default-column-width { proportion 0.5; }
}
binds {
    Mod+Return { spawn "owendots" "launch" "terminal"; }
    Mod+Shift+B { spawn "owendots" "launch" "browser"; }
    Mod+Shift+E { spawn "owendots" "launch" "files"; }
    Mod+D { spawn "walker"; }
    Mod+Shift+V { spawn "owendots" "clipboard"; }
    Mod+Comma { spawn "owendots" "menu"; }
    Mod+Q { close-window; }
    Mod+H { focus-column-left; }
    Mod+L { focus-column-right; }
    Mod+J { focus-window-down; }
    Mod+K { focus-window-up; }
    Mod+Shift+H { move-column-left; }
    Mod+Shift+L { move-column-right; }
    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Print { screenshot; }
    Mod+Shift+S { screenshot; }
    XF86AudioRaiseVolume allow-when-locked=true { spawn "wpctl" "set-volume" "-l" "1.0" "@DEFAULT_AUDIO_SINK@" "5%+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%-"; }
    XF86AudioMute allow-when-locked=true { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SINK@" "toggle"; }
    XF86AudioMicMute allow-when-locked=true { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SOURCE@" "toggle"; }
    XF86PowerOff { spawn "owendots" "power"; }
}
